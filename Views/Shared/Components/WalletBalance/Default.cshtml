@model WalletViewModel

@if (Model.Balance > 0.00M)
{
    <li class="nav-item">
        <a id="winnings" class="nav-link text-white" asp-controller="Withdraw" asp-action="Index"> Balance: €@Model.Balance</a>
    </li>
}